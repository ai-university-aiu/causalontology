# Archive

Superseded versions of versioned documents live here, per the ARCHIVE RULE in
[`GOVERNANCE.md`](../GOVERNANCE.md): when a new version of a versioned document
is created, the superseded version is moved into this directory **in the same
change**, and only the latest version of each series lives outside it. Nothing
is deleted — the lineage is the record.
