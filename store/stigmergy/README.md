# Stigmergy: gap signals and the contribution dashboard

Planned — roadmap step 4. Implements `GET /gaps` with the six gap kinds
(`missing_field`, `dangling_reference`, `empty_mechanism`,
`inconsistent_hierarchy`, `conflict`, `demand_supply`) and the minimal
dashboard that renders them for people. Gap-signals are just data, so
autonomous minds (such as Mentova) read the same frontier and contribute back
signed, intervention-grade refinements.
