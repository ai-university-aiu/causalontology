{
  "vector": "V118",
  "name": "realized_by_is_identity_bearing_absent_id_unchanged",
  "group": "W - realized_by reference (3.0.0)",
  "operation": "see bindings/*/conformance runner",
  "expect": {
    "realized_by_id_differs_from_unbound": true,
    "unbound_conduit_id_unchanged_from_2_0_0": true
  },
  "note": "3.0.0 Change Three: realized_by is identity-bearing, so a conduit that names it has a distinct identifier from one that does not; and a conduit that carries no realized_by keeps its 2.0.0 identifier byte-for-byte (additive, identity-preserving)."
}
