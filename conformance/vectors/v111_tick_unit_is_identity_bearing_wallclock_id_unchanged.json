{
  "vector": "V111",
  "name": "tick_unit_is_identity_bearing_wallclock_id_unchanged",
  "group": "T - ordinal tick temporal unit (3.0.0)",
  "operation": "see bindings/*/conformance runner",
  "expect": {
    "tick_id_differs_from_seconds_id": true,
    "wallclock_id_unchanged_from_2_0_0": true
  },
  "note": "3.0.0 Change One: the temporal unit is identity-bearing, so a tick-unit CRO has a distinct identifier from an otherwise-identical seconds-unit CRO; and an existing wall-clock CRO's identifier is unchanged under 3.0.0 (additive, identity-preserving)."
}
