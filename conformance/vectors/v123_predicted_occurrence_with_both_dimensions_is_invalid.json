{
  "vector": "V123",
  "name": "predicted_occurrence_with_both_dimensions_is_invalid",
  "group": "X - prediction and prediction error (4.0.0)",
  "operation": "see bindings/*/conformance runner",
  "expect": {
    "schema_valid": true,
    "semantically_valid": false,
    "gap": "dimension_conflict",
    "hard": true
  },
  "note": "4.0.0 Rule 24: a predicted interval carrying BOTH a wall-clock start and an ordinal start_tick is schema-valid but semantically invalid (dimension_conflict) - per Rule 23 the two dimensions never compare, so a window in both is incoherent. HARD."
}
