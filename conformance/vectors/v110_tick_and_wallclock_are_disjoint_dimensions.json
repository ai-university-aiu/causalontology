{
  "vector": "V110",
  "name": "tick_and_wallclock_are_disjoint_dimensions",
  "group": "T - ordinal tick temporal unit (3.0.0)",
  "operation": "see bindings/*/conformance runner",
  "expect": {
    "tick_delay_within_wallclock_window": false,
    "tick_and_wallclock_windows_overlap": false,
    "to_seconds_of_ticks_refused": true
  },
  "note": "3.0.0 Change One: an ordinal (tick) unit and a wall-clock unit are DIFFERENT DIMENSIONS - a tick delay is never within a wall-clock window, their windows never overlap, and converting ticks to seconds is a refused category error."
}
