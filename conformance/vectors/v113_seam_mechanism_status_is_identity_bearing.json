{
  "vector": "V113",
  "name": "seam_mechanism_status_is_identity_bearing",
  "group": "U - managed cross-stratal seam (3.0.0)",
  "operation": "see bindings/*/conformance runner",
  "expect": {
    "absent_is_valid": true,
    "unmodeled_and_absent_have_distinct_identity": true
  },
  "note": "3.0.0 Change Two: mechanism_status is identity-bearing - a seam declaring 'absent' (no intervening mechanism exists) has a DISTINCT identifier from one declaring 'unmodeled' (a mechanism exists but is not drawn). This is exactly the honest-ignorance distinction the bare skips boolean could not make."
}
