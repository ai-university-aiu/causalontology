{
  "vector": "V120",
  "name": "predicted_occurrence_with_tick_window_is_valid",
  "group": "X - prediction and prediction error (4.0.0)",
  "operation": "see bindings/*/conformance runner",
  "expect": {
    "schema_valid": true,
    "semantically_valid": true,
    "id_scheme_is_predicted_occurrence": true,
    "id_differs_from_token_occurrence_over_comparable_fields": true
  },
  "note": "4.0.0 Section A: a predicted_occurrence over an ordinal tick window is schema- and semantically valid; its identifier carries the predicted_occurrence: scheme and DIFFERS from the token_occurrence identifier minted over comparable fields - a forecast is not a report (Rule 24)."
}
