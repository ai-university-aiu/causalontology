{
  "vector": "V117",
  "name": "conduit_realized_by_is_valid",
  "group": "W - realized_by reference (3.0.0)",
  "operation": "see bindings/*/conformance runner",
  "expect": {
    "schema_valid": true
  },
  "note": "3.0.0 Change Three: a conduit carrying a realized_by reference (naming, by identity, the native law or signal that realizes its transform) is schema-valid; it is invalid under 2.0.0 (additionalProperties false), correctly a 3.0.0 feature."
}
