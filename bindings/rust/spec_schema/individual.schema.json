{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://causalontology.org/schema/individual.schema.json",
  "title": "Token Individual",
  "type": "object",
  "required": [
    "id",
    "type",
    "instantiates"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^token_individual:[0-9a-f]{64}$"
    },
    "type": {
      "const": "token_individual"
    },
    "instantiates": {
      "type": "string",
      "pattern": "^continuant:[0-9a-f]{64}$",
      "description": "the continuant TYPE of which this is an instance; MANDATORY (P1)"
    },
    "designator": {
      "type": "string",
      "minLength": 1,
      "description": "an OPAQUE local designator distinguishing this individual from others of its type. SEE THE PRIVACY REQUIREMENT, N5.1.3 / spec/safety.md."
    },
    "part_of": {
      "type": "string",
      "pattern": "^token_individual:[0-9a-f]{64}$",
      "description": "optional: the particular individual of which this is a part (TOKEN mereology)"
    }
  }
}
